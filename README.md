# An IoT Application that reads values through sensors.

The values are send via LoRaWan to the TTN Network, get read by a PI which acts as the DB and is connected a the WIFI - from there via Server Send Events the frontend dashboard gets updated automatically.  